---
title: Orders
description: Order lifecycle, checkout with address snapshot, admin management, dashboard.
icon: ShoppingCart
---

Order lifecycle management including checkout, customer order viewing, admin status management, and dashboard statistics.

## Endpoints

### Customer

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| `POST` | `/orders/checkout` | WithAuth | Checkout with address + screenshot |
| `GET` | `/orders/my` | WithAuth | List my orders |
| `GET` | `/orders/my/{id}` | WithAuth | Get my order |
| `DELETE` | `/orders/my/{id}` | WithAuth | Cancel my order (pending only) |

### Admin

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| `GET` | `/admin/orders` | Admin | List all orders (filterable) |
| `GET` | `/admin/orders/{id}` | Admin | Get order details |
| `PATCH` | `/admin/orders/{id}/status` | Admin | Update order status |
| `GET` | `/admin/dashboard` | Admin | Dashboard stats |

## Checkout (multipart/form-data)

| Field | Type | Required |
|-------|------|----------|
| `address_id` | int | Yes |
| `items` | JSON string | Yes |
| `payment_screenshot` | file | No |
| `payment_reference` | string | No |

Items JSON format:

```json
[{"productId": 1, "productName": "Sparkler Pack", "price": 99, "quantity": 2}]
```

## Order Status Transitions

```text
pending ──→ confirmed ──→ shipped ──→ delivered
  │            │            │
  └──→ cancelled ←──→ cancelled ←──→ cancelled
```

Only pending orders can be cancelled by customers. Admin can transition to any valid next state.

## Dashboard Stats

```json
{
  "totalOrders": 150,
  "pendingOrders": 12,
  "revenueMonth": 45000.00,
  "newCustomers": 28
}
```
